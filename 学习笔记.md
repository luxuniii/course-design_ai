# 随机森林质量分类模型训练调优学习笔记

> 课题：半导体产线智能质量预警系统（SECOM 数据集・随机森林二分类）
> 主题：从「准确率 / 召回率全为 1」的假象，到真实可汇报的泛化指标
> 本文所有数值均来自本次实际调参实验



***

## 一、问题背景



* **任务**：基于 423 维传感器特征，二分类预测半导体批次质量（0 = 合格 / 1 = 失效）。

* **数据分布**（数据库 `production_batch` 表）：



| 划分    | 总批次  | 失效批次 | 失效占比 |
| ----- | ---- | ---- | ---- |
| train | 1096 | 78   | 7.1% |
| val   | 314  | 23   | 7.3% |
| test  | 157  | 3    | 1.9% |



* **原始特征** 590 维，预处理删除全缺失列后剩 423 维。

* **现象**：训练完成后，准确率、召回率、F1 全部等于 1 → **明显不合理的 "满分"**。



***

## 二、核心概念一：训练集自评估陷阱（为什么指标全是 1）

### 2.1 错误代码



```
X, y = get\_train\_dataset()

model.fit(X, y)            # 用全部数据训练

y\_pred = model.predict(X)  # 又用同一批数据预测

\# 准确率/召回率都在这批"见过答案"的数据上计算
```

### 2.2 问题本质

随机森林是**强拟合模型**，对训练过的样本几乎能 "记住" 标签（近似记忆）。用训练数据评估，模型相当于**开卷考试**，指标恒接近 1，反映的是**拟合能力**，而非**泛化能力**。

> **关键结论：指标必须建立在模型 "没见过" 的数据上才有意义。**

### 2.3 诊断信号



* 模型在训练集上完美分类（失效概率中位数 0.93 vs 合格 0.06）

* 一旦换成新数据，性能大幅跳水

* 这就是 "过了一会召回率又变回 1" 的真正原因 ——**评估对象仍是训练集本身**（代码回退到自评估版本）



***

## 三、核心概念二：训练 / 验证集划分（train\_test\_split）

### 3.1 正确写法



```
from sklearn.model\_selection import train\_test\_split

X\_train, X\_eval, y\_train, y\_eval = train\_test\_split(

&#x20;   X, y, test\_size=0.2, random\_state=42, stratify=y

)

model.fit(X\_train, y\_train)       # 只在训练部分拟合

y\_pred = model.predict(X\_eval)    # 只在未见过的部分评估
```

### 3.2 关键参数



| 参数                | 作用                                          |
| ----------------- | ------------------------------------------- |
| `test_size=0.2`   | 切出 20% 作为评估集                                |
| `random_state=42` | 固定随机种子，结果可复现                                |
| `stratify=y`      | **分层抽样**，保证训练 / 评估集里失效样本占比一致（避免评估集恰好没有失效样本） |

### 3.3 划分后发生了什么

划分后召回率从 1 骤降到 0 → **真实的泛化能力被暴露**：模型把所有批次都判为合格，失效样本全部漏掉。这正是数据集不平衡 + 默认阈值 0.5 共同导致的。



***

## 四、核心概念三：数据不平衡问题

### 4.1 问题描述

失效样本仅占 7.1%，模型为了 "整体正确率最高"，会倾向把**所有样本都判为多数类（合格）**，导致：



* 准确率虚高（约 92%）

* 召回率 = 0（失效全漏）

### 4.2 验证方法



```
print(y.value\_counts())   # 观察类别分布

\# 0    1018

\# 1      78
```



***

## 五、核心概念四：召回率 vs 准确率（工业场景的取舍）

### 5.1 混淆矩阵



```
&#x20;             预测合格   预测失效

真实合格        TN=120    FP=84

真实失效        FN=4      TP=12
```

### 5.2 指标公式



| 指标           | 公式                    | 含义              |
| ------------ | --------------------- | --------------- |
| 准确率 Accuracy | (TP+TN)/(TP+TN+FP+FN) | 整体判对比例          |
| 召回率 Recall   | TP/(TP+FN)            | **真实失效中被找出的比例** |
| F1           | 2·P·R/(P+R)           | 精确率与召回率的调和平均    |

### 5.3 工业预警原则

质量预警的场景是 \*\*"宁可误报、不可漏报"**—— 漏掉一个失效批次，损失远大于误报一个合格批次。因此**召回率是首要指标 \*\*，准确率可以适当让步。

> 实现方式：不直接用默认 0.5 阈值，而是把分类阈值调低，提高失效判定灵敏度。



***

## 六、核心概念五：自定义分类阈值

### 6.1 原理

`model.predict()` 默认在概率 ≥ 0.5 时判为失效；改用 `predict_proba()` 拿到失效类概率，再与自定义阈值比较：



```
y\_proba = model.predict\_proba(X\_eval)\[:, 1]   # 失效类概率

threshold = 0.20                              # 自定义阈值

y\_pred = (y\_proba >= threshold).astype(int)
```

### 6.2 阈值与指标的关系（本次实测数据）



| 阈值       | 准确率       | 召回率       | F1        |
| -------- | --------- | --------- | --------- |
| 0.10     | 0.227     | 0.875     | 0.141     |
| 0.15     | 0.423     | 0.812     | 0.170     |
| **0.20** | **0.600** | **0.750** | **0.214** |
| 0.25     | 0.727     | 0.500     | 0.211     |
| 0.30     | 0.795     | 0.312     | 0.182     |
| 0.40     | 0.914     | 0.125     | 0.174     |
| 0.50     | 0.927     | 0.000     | 0.000     |

> **规律**
>
> ：阈值越低 → 召回率越高、准确率越低（误报增多）；阈值越高 → 反之。两者永远此消彼长，不存在 "两个都高"。
> **注意**
>
> ：训练评估与单样本预测必须使用
>
> **同一阈值**
>
> ，否则判定标准不一致。



***

## 七、核心概念六：SMOTE 过采样

### 7.1 为什么需要

类别不平衡严重时，仅靠 `class_weight='balanced'` 仍不够，模型对失效类的概率输出普遍偏低（本次实测失效样本概率最高仅 0.25）。

### 7.2 SMOTE 原理

对少数类样本，在其 k 近邻之间**插值合成新样本**，人工扩充失效样本数量，让模型学到更充分的失效模式。



```
from imblearn.over\_sampling import SMOTE

smote = SMOTE(random\_state=42, k\_neighbors=3)

X\_bal, y\_bal = smote.fit\_resample(X\_train, y\_train)  # 只对训练部分过采样

model.fit(X\_bal, y\_bal)
```

### 7.3 红线：不能污染评估集

SMOTE **只能作用于训练集**。若先对全量数据过采样再划分，合成的 "失效样本" 会泄漏进评估集，造成假的高召回率（数据泄露）。



***

## 八、核心概念七：过拟合（高维噪声特征）

### 8.1 症状



* 训练集上：失效概率中位数 0.93，合格 0.06（区分完美）

* 验证集上：失效 0.17，合格 0.14（几乎无区分）

  → 模型**记住了训练集的噪声模式**，无法泛化。

### 8.2 成因

423 维高噪声特征 + 仅 78 个失效样本 + 默认深树 + SMOTE 放大 → 模型容量远超数据信息量。

### 8.3 缓解手段



```
RandomForestClassifier(

&#x20;   n\_estimators=200,

&#x20;   max\_depth=12,          # 限制树深

&#x20;   min\_samples\_leaf=5,    # 叶子最少样本数

&#x20;   max\_features='sqrt',   # 每棵树只看部分特征，增加多样性

&#x20;   class\_weight='balanced',

&#x20;   random\_state=42

)
```

### 8.4 过拟合诊断方法（重要技能）

对比同一模型在训练集和验证集上的失效概率分布：



```
p\_train = model.predict\_proba(X\_train)\[:, 1]

p\_val   = model.predict\_proba(X\_eval)\[:, 1]

\# 训练集失效概率高、验证集低 → 过拟合
```



***

## 九、调试实战复盘（完整时间线）



| 阶段 | 代码状态                    | 召回率      | 原因                      |
| -- | ----------------------- | -------- | ----------------------- |
| 1  | 全量数据训练 + 全量自评估          | 1（假象）    | 训练集自评估，开卷考试             |
| 2  | 加 train\_test\_split 划分 | 0        | 暴露真实不平衡问题，默认阈值下失效全漏     |
| 3  | 降阈值到 0.3                | 0        | 模型对失效类概率普遍 < 0.3，仍全判合格  |
| 4  | SMOTE + 保守 RF + 阈值 0.2  | **0.75** | 平衡采样 + 防过拟合 + 调低阈值，三者协同 |

### 最终落地配置（已写入 `src/algorithms/quality_predict.py`）



* 训练：train 集内部 8:2 随机划分（stratify）

* 采样：SMOTE (k\_neighbors=3) 仅作用于训练部分

* 模型：RandomForest（200 棵树，max\_depth=12，min\_samples\_leaf=5，max\_features='sqrt'，class\_weight='balanced'）

* 阈值：0.20

* **最终指标：召回率 0.75 / 准确率 0.60 / F1 0.21**（混淆矩阵 \[\[120,84],\[4,12]]）



***

## 十、经验总结与可复用清单

### 10.1 五大教训



1. **永远不要用训练数据评估模型**—— 指标必须来自模型未见过的数据。

2. **召回率 = 1 且准确率 = 1 是危险信号**，先怀疑评估逻辑，不是模型 "太强"。

3. **不平衡数据**优先查类别分布（`value_counts`），别被整体准确率误导。

4. **工业预警**优先保证召回率，用阈值调节 "漏报 / 误报" 的取舍。

5. **过采样 / 过拟合**都要有边界意识：SMOTE 只碰训练集，模型容量要匹配数据量。

### 10.2 排错 Checklist（下次直接复用）



```
□ 指标是不是在训练集上算的？→ 检查 predict 用的是 X 还是 X\_test

□ 测试集/评估集里有没有失效样本？→ stratify 是否设置

□ 特征矩阵是否混入了 final\_result 标签列？→ 列数是否为 423

□ SMOTE 是否污染了评估集？→ 是否先划分再过采样

□ 阈值在训练和预测两处是否一致？→ 两处都要改

□ 模型是否过拟合？→ 对比训练集/验证集概率分布
```



***

*学习笔记完・数据来源：本次 SECOM 随机森林调参实验（Python310 /sklearn 1.2.2 /imblearn 0.10.1）*