# 方案设计
## 1 功能需求分析
### 1.1 功能性需求
1. **时序数据管理功能**
支持电机皮带传动时序数据集导入；完成缺失值填充、异常过滤、降噪、滑动窗口特征提取；将原始数据、处理后特征数据集存入MySQL数据库。
2. **故障预测功能**
调用算法模块，基于电机时序特征完成多分类故障预测；输出故障类别、预警等级；支持随机森林主模型、LSTM备选模型两种预测方式。
3. **状态可视化功能**
前端实现电机电流、振动、转速时序曲线绘制；故障类型统计柱状图、饼图看板；设备运行状态总览。
4. **运维工单管理功能**
故障预警触发后可新建运维工单；填写设备编号、故障类型、预警时间、检修内容；支持工单状态：待处理、处理中、已完成；支持工单修改、归档。
5. **历史查询功能**
查询历史设备运行时序记录、历史故障预警记录、全部运维工单台账，支持按设备、时间筛选检索。

### 1.2 非功能性需求
1. B/S架构，浏览器直接访问，无需客户端安装；
2. Demo系统可完整演示，算法预测结果可复现；
3. 后端接口具备异常容错，非法数据输入不会直接造成系统崩溃；
4. 项目开发全程Git小步提交，开发过程可追溯；
5. 代码具备基础注释，预留单元测试，便于调试。

## 2 方案论证
### 2.1 业务场景论证
皮带传动是流水线、数控机床的关键传动部件，早期故障隐蔽，传统人工巡检很难发现隐患。基于电机采集的时序信号，使用统计学习方法实现故障预测，能够实现预测性维护，降低停机损失。课题场景贴合制造智能技术课程工业智能运维方向。

### 2.2 算法方案论证
- **主模型‑随机森林**：教材第6章集成学习，对表格化时序统计特征效果好，训练速度快、调参简单、推理速度快，适合网页后端调用，可解释性强，答辩便于讲解。
- **备选模型‑LSTM**：教材第7章循环神经网络，直接处理原始时序序列，挖掘时间依赖关系，用于和随机森林做结果对比。


### 2.3 开发模式论证
采用Vibe‑Coding规格驱动AI辅助开发，进行任务拆解、提示词约束、上下文迭代、AI生成+人工审查+单元测试，留存Prompt日志；Git小步提交保障开发过程可追溯，满足课程过程考核。

## 3 方案规划
### 3.1 技术栈
- 后端语言：Python
- Web框架：Flask，提供RESTful API
- 数据库：MySQL，存储时序数据、故障记录、工单
- 算法库：scikit‑learn（随机森林）、TensorFlow/Keras（LSTM）、numpy、pandas（数据预处理）
- 前端：HTML+JavaScript+ECharts，实现可视化看板、表单交互
- 版本管理：Git，远程代码仓库
- AI辅助开发工具：通义灵码（Harness工具）

### 3.2 需要配置外部资源
1. UCI Belt drive fault公开数据集下载；
2. MySQL本地数据库环境；
3. Python依赖环境：Flask、pandas、numpy、scikit‑learn、tensorflow、pymysql；
4. Git仓库，用于提交选题说明.md、方案设计.md、代码、测试脚本。

### 3.3 数据集来源
UCI Machine Learning Repository — Belt drive fault数据集。
采集电机‑皮带传动装置真实运行时序信号，包含振动、电流、转速多维度时序，类别：正常、皮带松弛、皮带磨损、皮带故障前兆。

### 3.4 前端UI方案
1. 首页总览看板：设备状态统计、故障数量统计图表；
2. 数据管理页面：数据集上传、数据预览、预处理操作；
3. 状态监测页面：电机时序曲线展示、故障预测结果展示；
4. 运维工单页面：工单列表、新增工单、编辑工单状态；
5. 历史查询页面：历史故障、历史工单检索查询。

### 3.5 后端方案
四层B/S架构
1. 前端UI层：网页交互、图表可视化；
2. 后端服务层：Flask API，接收前端请求，执行业务逻辑，调度算法模块，读写数据库；
3. 数据库层：MySQL，数据表包含：设备信息表、原始时序数据表、特征数据集表、故障预警记录表、运维工单表；
4. AI算法模块：读取数据库数据，完成特征工程，模型推理，输出故障预测结果。

### 3.6 算法方案
1. 数据预处理模块：缺失填充、异常值剔除、滑动窗口提取时域特征（均值、方差、均方根等）；
2. 随机森林分类模型：输入窗口统计特征，输出4类故障分类结果；
3. LSTM时序模型：输入原始时序片段，输出故障分类，作为对比实验；
4. 模型输出结果回传给后端接口，推送至前端页面展示预警。

## 4 技术路线
1. 前期阶段：选题确认，完成选题说明.md、方案设计.md，推送至Git仓库；
2. 数据阶段：获取数据集，完成清洗、特征工程，导入MySQL数据库；
3. 算法阶段：训练随机森林、LSTM模型，完成模型效果验证，编写预测推理脚本；
4. 后端开发：Flask接口开发，实现数据、预测、工单业务接口，对接数据库与算法模块；
5. 前端开发：开发各Web页面，ECharts可视化，前后端接口联调；
6. 测试调试：单元测试、整体系统测试，修复Bug，达到可演示状态；
7. 结题阶段：整理全部过程文档，录制演示视频，制作答辩PPT。

## 5 计划安排（共10工作日）
1. 第1‑2天：选题确定，编写选题说明.md、方案设计.md，提交推送Git仓库（本阶段任务）
2. 第3天：数据集下载、预处理、特征工程，数据入库，数据库表设计
3. 第4‑5天：算法模块开发，随机森林、LSTM模型训练、验证
4. 第6‑7天：Flask后端API开发，完成全部业务接口
5. 第8天：前端页面开发，可视化实现，前后端联调
6. 第9天：系统集成测试，Bug修复，整理Prompt日志、Git提交记录等全部过程资料
7. 第10天：撰写完整课程设计报告，录制演示视频，完成答辩PPT

## 6 参考文献
[1] 人工智能基础[M]. 本书编写组. 高等教育出版社.
> 参考章节：第5章 优化与智能计算；第6章 统计学习方法（集成学习、随机森林）；第7章 深度学习（循环神经网络、LSTM），为本课题算法模型、优化方法提供理论基础。

[2] 本书编写组.人工智能基础 第8章强化学习、第9章自然语言处理、第10章智能机器人[M].高等教育出版社.




