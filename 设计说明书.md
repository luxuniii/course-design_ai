# 半导体产线智能质量预警系统 设计说明书

## 摘要

本设计面向半导体制造过程质量管控需求，基于 UCI 公开的 SECOM 半导体生产数据集，构建了一套 B/S 架构的智能质量预警系统。系统以 Flask 提供后端服务、SQLite 存储数据，集成了 PCA 高维降维、孤立森林异常检测、随机森林质量预测三类算法，实现了生产批次管理、高维工艺特征降维可视化、工艺异常检测、产品质量提前预测等功能。系统覆盖了从原始数据清洗、数据库构建、算法训练到前端交互的完整开发链路，可为半导体制造场景的质量预警提供一套轻量级的工程化方案。



***

## 一、背景调研（研究意义）

### 1.1 行业背景

半导体晶圆制造是工序最多、工艺最复杂的工业过程之一。一片晶圆从投片到出厂要经过上百道工序，期间温度、压强、气体流量、腔体参数等各类工艺传感器会持续产生数百维的测量数据。这些数据里其实藏着批次质量的线索 —— 很多失效批次在最终测试之前，工艺参数就已经出现了偏离。

传统质量管控以事后检测为主，等终检发现失效，前面的成本已经全部投入。如果能用机器学习提前从工艺数据里发现异常趋势，就可以减少无效生产、提前干预，这正是智能制造里 "质量预测性维护" 的方向。

### 1.2 问题提出

半导体生产数据有几个特点，让它不适合直接用传统统计过程控制（SPC）方法处理：



1. **高维度**：单个批次就有 590 维传感器特征，人工观察不现实，传统统计控制图也难覆盖；

2. **样本不均衡**：失效批次占比只有 6% 左右，正常批次占绝对多数，模型很容易 "学会" 全判合格；

3. **噪声大**：工业现场采集的数据存在大量缺失值和异常点，直接喂给模型效果很差。

同时，工业场景需要的是一套**可视、可交互的软件系统**来承载算法能力，而不是一个孤立的离线脚本。这也是本设计选择做完整 Web 系统而不是只跑算法的原因。

### 1.3 设计目标



1. 建立完整的工业数据处理与存储流程，完成 SECOM 数据集的清洗、特征筛选、标准化与结构化入库；

2. 集成三类典型工业智能算法 ——PCA 降维、孤立森林异常检测、随机森林质量预测，覆盖 "降维可视化 — 异常预警 — 质量判定" 三个层次；

3. 开发 B/S 架构 Web 应用，提供批次查询、一键智能分析、结果可视化等交互功能；

4. 按工程化规范开发，代码分层清晰，数据与训练流程可复现。



***

## 二、方案设计

### 2.1 总体架构

系统采用经典的三层 B/S 架构：



```
浏览器（HTML + Bootstrap + JavaScript）

&#x20;       │  HTTP 请求 / JSON 响应

&#x20;       ▼

Flask 后端服务层（路由控制、算法调度、业务逻辑）

&#x20;       │  统一 db\_api 封装

&#x20;       ▼

SQLite 数据层（production\_batch / sensor\_measurement / algorithm\_result）
```



* **表现层**：Web 页面，负责数据展示与交互，使用 ECharts 做图表可视化；

* **业务逻辑层**：Flask 提供 RESTful API，统一 `code / msg / data` 返回格式，负责调度算法模块、读写数据库；

* **数据层**：SQLite 关系型数据库，存储批次信息、传感器测量数据、算法分析结果三类核心数据。

### 2.2 技术选型



| 层级   | 选型                                            | 选型理由                                    |
| ---- | --------------------------------------------- | --------------------------------------- |
| 后端框架 | Flask 2.x                                     | 轻量、上手快，适合课程设计规模的后端服务                    |
| 数据库  | SQLite 3                                      | 单机免部署，1567 批次的数据量完全够用，免去 MySQL 环境配置     |
| 前端   | HTML5 + Bootstrap 5 + 原生 JavaScript + ECharts | 无需构建工具，页面开发直观                           |
| 算法库  | scikit-learn                                  | PCA、IsolationForest、RandomForest 均有成熟实现 |
| 数据处理 | Pandas + NumPy                                | 表格数据处理与数值计算的标准工具                        |
| 版本管理 | Git                                           | 按阶段小步提交，过程可追溯                           |

### 2.3 功能模块划分

系统划分为四个功能模块：



1. **数据预处理模块**：原始数据清洗、缺失值填充、异常值处理、特征标准化、数据集划分；

2. **数据库模块**：三张核心表设计、数据入库、统一数据访问接口；

3. **智能算法模块**：PCA 降维、孤立森林异常检测、随机森林质量预测三个独立算法单元；

4. **Web 交互模块**：首页、批次列表页、批次详情页三个页面单元。

### 2.4 开发模式

采用规格驱动的 AI 辅助开发模式：先拆解任务、明确每个模块的输入输出与验收标准，再分步实现、逐模块测试。全程用 Git 小步提交，AI 交互过程记录存放在 `prompt/` 目录，便于课程过程考核与追溯。



***

## 三、数据集构建

### 3.1 数据来源

数据集选用 UCI 机器学习仓库公开的 **SECOM 半导体制造数据集**（[https://archive.ics.uci.edu/ml/datasets/SECOM](https://archive.ics.uci.edu/ml/datasets/SECOM)）。



| 项目   | 说明                        |
| ---- | ------------------------- |
| 样本规模 | 1567 个生产批次                |
| 特征维度 | 590 个工艺传感器测量特征            |
| 标签   | 每批次最终质量检测结果（-1 合格 / 1 失效） |
| 失效占比 | 约 6.6%                    |

数据包含三个文件：`secom.data`（传感器测量主数据）、`secom_labels.data`（批次质量标签）、`secom.names`（特征说明）。

### 3.2 数据预处理流程

预处理脚本为 `src/data_preprocess.py`，处理流程分六步：



1. **加载与标签映射**：原始标签 -1（合格）/ 1（失效）映射为 0 / 1，便于后续建模；

2. **高缺失列剔除**：缺失率超过 30% 的特征列直接删除 —— 这类列有效信息太少，强行填充反而引入噪声；

3. **中位数填充**：剩余缺失值用该列中位数填充。选择中位数而不是均值，是因为均值容易被异常值拉偏，中位数更稳健；

4. **异常值处理（3σ 准则）**：超过 "均值 ± 3 倍标准差" 的数值替换为该列中位数，抑制极端离群点的影响；

5. **低方差列剔除 + Z-score 标准化**：剔除方差接近 0 的无信息列（这些列所有批次取值几乎相同，对分类没有贡献），再对保留特征做标准化，消除量纲差异；

6. **数据集划分**：按批次原始顺序以 7:2:1 划分为训练集 / 验证集 / 测试集。

经过清洗，特征从 590 维缩减到 423 维（剔除高缺失列约 130+ 列、低方差列若干），缺失率降为 0。

### 3.3 数据集划分



| 划分        | 批次数量 | 失效数量 | 失效占比 |
| --------- | ---- | ---- | ---- |
| 训练集 train | 1096 | 78   | 7.1% |
| 验证集 val   | 314  | 23   | 7.3% |
| 测试集 test  | 157  | 3    | 1.9% |

划分时按原始顺序切分（前 70% / 70%～90% / 后 10%）。训练集负责训练模型，验证集用于调参和选择阈值，测试集留作最终评估。

### 3.4 数据入库

处理后的数据写入 SQLite，数据库设计三张核心表：



| 表名                  | 主要字段                                                                         | 作用                |
| ------------------- | ---------------------------------------------------------------------------- | ----------------- |
| production\_batch   | batch\_id、batch\_code、final\_result、split\_type                              | 批次基础信息与质量标签       |
| sensor\_measurement | batch\_id、sensor\_no、sensor\_value                                           | 每个批次的传感器测量值（长表存储） |
| algorithm\_result   | batch\_id、pca\_x、pca\_y、anomaly\_score、risk\_level、predict\_label、confidence | 每次智能分析的结果         |

`sensor_measurement` 采用 "批次 × 传感器编号" 的长表结构，查询时再转成宽表特征矩阵，避免单行字段过多。数据库访问统一封装在 `src/db_api.py`，业务层不直接写 SQL。



***

## 四、三个技术方向的详细设计

### 4.1 技术方向一：工业数据预处理与特征工程

#### 设计思路

工业数据的质量直接决定模型上限。SECOM 数据有大量缺失和离群值，如果原样喂给模型，模型学到的会是噪声而不是规律。预处理的目标是：剔除无效信息、填充缺失、压缩异常、统一量纲，最终得到一份干净、可用的特征矩阵。

#### 关键实现



```
\# 1. 缺失率 > 30% 的列剔除

missing\_ratio = df.isnull().mean()

drop\_cols = missing\_ratio\[missing\_ratio > 0.3].index.tolist()

df = df.drop(columns=drop\_cols)

\# 2. 剩余缺失值用中位数填充

df = df.fillna(df.median(numeric\_only=True))

\# 3. 3σ 异常值替换为中位数

df\[col] = np.where(

&#x20;   (df\[col] < mean - 3\*std) | (df\[col] > mean + 3\*std),

&#x20;   df\[col].median(), df\[col]

)

\# 4. 剔除低方差列 + Z-score 标准化

df = df.drop(columns=low\_var\_cols)

df\[feature\_cols] = StandardScaler().fit\_transform(df\[feature\_cols])
```

#### 处理效果



* 特征维度：590 → 423；

* 缺失率：原始存在大量缺失 → 0；

* 标准化后各特征均值 0、方差 1，量纲统一，模型可以正常收敛。

### 4.2 技术方向二：机器学习质量预测（随机森林）

#### 设计思路

质量预测的目标是在生产过程中根据传感器数据提前判断批次 "合格 / 失效"。选择随机森林的原因：适合高维结构化特征、对噪声鲁棒、训练速度快、调参简单，且能输出概率，便于和自定义阈值结合做工业决策。

#### 类别不平衡处理（SMOTE）

失效样本仅占 7%，直接训练模型会严重偏向多数类。项目采用两步缓解：



1. 训练时使用 `class_weight='balanced'`，自动给少数类更高权重；

2. 对训练集做 **SMOTE 过采样**—— 在失效样本的 k 近邻之间插值合成新样本，把失效样本数量提升到和合格样本相当。

需要强调的是，**SMOTE 只作用于训练集**。如果先对全量数据过采样再划分，合成的样本会泄漏进评估集，得到虚高的指标（数据泄露）。

#### 防过拟合设计

SECOM 特征维度高（423 维）而失效样本少（78 个），直接训练默认参数的随机森林会严重过拟合 —— 表现为训练集上完美分类、验证集上几乎失效。因此对模型做了约束：



```
RandomForestClassifier(

&#x20;   n\_estimators=200,        # 200 棵树

&#x20;   max\_depth=12,            # 限制树深

&#x20;   min\_samples\_leaf=5,      # 叶子最少样本数

&#x20;   max\_features='sqrt',     # 每棵树只看部分特征，增加多样性

&#x20;   class\_weight='balanced',

&#x20;   random\_state=42

)
```

#### 自定义分类阈值

默认的 0.5 分类阈值在类别不平衡数据上会导致召回率归零（模型把所有批次都判为合格）。考虑到工业质检 "宁可误报、不可漏报" 的原则，改用 `predict_proba` 输出失效概率，再和自定义阈值比较：



```
y\_proba = model.predict\_proba(X\_eval)\[:, 1]   # 失效类概率

threshold = 0.20

y\_pred = (y\_proba >= threshold).astype(int)
```

训练评估和单批次预测两处使用**同一个阈值**，保证判定标准一致。

#### 模型评估

评估方式：从训练集中按 8:2 随机划分出独立评估子集（分层抽样保持失效比例），模型只在训练部分拟合，指标只在模型从未见过的评估子集上计算。



| 指标  | 数值   |
| --- | ---- |
| 召回率 | 0.75 |
| 准确率 | 0.60 |
| F1  | 0.21 |

混淆矩阵（评估子集：合格 204 个，失效 16 个）：



```
&#x20;           预测合格   预测失效

真实合格       120       84

真实失效         4       12
```

12 个失效批次被成功检出 12 个（TP=12，FN=4），召回率 0.75。准确率 0.60 偏低是阈值下调后的正常代价 ——84 个合格批次被误报为失效，这是 "宁可多报" 的取舍结果。在缺陷检测场景，漏掉一个失效批次的损失远大于多一次复检，因此这个配置是合理的。

#### 调参过程记录

调试中走了不少弯路，也正好暴露了评估方法的重要性：



* **最初版本**：用全量训练数据训练，又用同一批数据计算指标，准确率、召回率全是 1—— 这是典型的训练集自评估假象，模型在 "开卷考试"，指标没有参考价值；

* **改为训练 / 评估分离后**：召回率跌到 0，暴露出真实的类别不平衡问题；

* **最终方案**：SMOTE 过采样 + 限制模型复杂度 + 自定义阈值 0.20 三者结合，才得到上面真实可信的指标。

### 4.3 技术方向三：异常检测与降维可视化（孤立森林 + PCA）

#### 孤立森林：无监督异常检测

设计思路：质量预测需要历史标签，但在产线实际场景里，新故障模式往往没有历史样本。孤立森林不需要标签，只根据数据分布判断 "这个批次是否偏离正常工艺"，适合做前置预警。

原理：通过随机切分特征空间构建孤立树，异常样本因为特征偏离大，更容易被少量几次切分就孤立出来（路径短），据此计算异常分数。项目实现：



```
\# 只用合格样本训练，让模型学习"什么是正常"

X\_normal = X\[y == 0]

model = IsolationForest(contamination=0.1, random\_state=42)

\# 单批次预测：decision\_function 转 0\~100 风险分

score = model.decision\_function(X)\[0]

risk\_score = float((-score + 0.5) \* 100)

risk\_score = max(0, min(100, risk\_score))
```

风险等级按分数划分：



| 风险得分      | 等级  |
| --------- | --- |
| 0 \~ 29   | 低风险 |
| 30 \~ 69  | 中风险 |
| 70 \~ 100 | 高风险 |

分数越高代表批次偏离正常工艺越严重。例如某批次风险得分 52.26，落在 30\~70 区间，判定为中风险，表示工艺参数有轻度偏离、值得关注。

#### PCA：高维特征降维可视化

423 维特征无法直接画图，项目用主成分分析（PCA）把特征压缩到 2 维主成分，在散点图上展示每个批次在特征空间的位置。第一主成分、第二主成分分别对应数据方差最大的两个方向，保留主要信息。前端把 PCA 坐标绘制成散点图，可以直观看到批次分布和异常点的相对位置。

#### 两个算法与随机森林的分工



| 算法   | 是否需标签 | 输出                   | 角色     |
| ---- | ----- | -------------------- | ------ |
| PCA  | 否     | 2 维投影坐标              | 可视化辅助  |
| 孤立森林 | 否     | 风险得分 + 等级            | 前置异常预警 |
| 随机森林 | 是     | 故障概率 + 置信度 + 合格 / 失效 | 最终质量判定 |

三者互补：PCA 让人 "看得见"，孤立森林解决 "没标签也能预警"，随机森林解决 "准确判定"。



***

## 五、界面设计

### 5.1 系统首页

首页展示系统名称、服务运行状态和功能导航入口，让使用者一进来就知道系统能做什么、从哪里进入。

### 5.2 批次列表页

以表格形式展示全部 1567 个生产批次，包含批次编号、质量结果、数据集类型等字段，合格 / 失效用不同状态标识，支持点击进入详情。

### 5.3 批次详情页

批次详情页是本系统的核心交互页面，布局分三块：



1. **基本信息区**：展示批次编号、真实质量结果、数据集类型，真实结果来自数据库，作为和预测结果对照的 "标准答案"；

2. **操作区**："重新执行分析" 按钮，点击后后端并行执行三个算法，分析结果写入数据库；

3. **算法结果区**：三个结果卡片并列展示 ——

* 高维特征降维（PCA）：第一 / 第二主成分坐标；

* 工艺异常检测（孤立森林）：风险得分与风险等级；

* 质量预测结果（随机森林）：预测结果与置信度。

以实际批次为例：某批次风险得分 52.26（中风险），随机森林输出故障概率 18.19%，低于阈值 20%，判定为合格，置信度 81.81%（= 1 - 18.19%）；该批次真实结果也为合格，预测与真实一致。三张卡片对应三个算法，每个数值都能在代码里找到计算来源。



***

## 六、系统调试

### 6.1 运行环境



* 操作系统：Windows 10/11

* Python：3.10

* 依赖：Flask、pandas 1.5.3、numpy 1.23.5、scikit-learn 1.2.2、imbalanced-learn 0.10.1

* 浏览器：Chrome / Edge

### 6.2 典型问题与解决过程

**1. 接口 500 错误**

手动录入批次接口调用时返回 500。排查思路：先看 Flask 终端堆栈，再用 "注释法" 逐步缩小范围 —— 先把算法部分注释掉、只保留数据库插入，确认问题出在数据库插入语句的字段不匹配上。定位后修正 SQL 字段即恢复。

**2. 模块导入错误**

清理代码时误删了 `db_api.py` 中的查询函数，导致 `app.py` 启动报 `ImportError`。处理方式：核对 `algorithms/__init__.py` 的导出清单，恢复完整的数据访问函数，保证导入链完整。

**3. 模型指标异常（本项目最大的调试点）**

现象：训练完成后准确率、召回率全部等于 1。

定位过程：



* 检查代码发现，模型用全量训练数据训练后，又用**同一批数据**计算指标 —— 这是训练集自评估，模型 "见过答案"，指标恒为满分，属于假象；

* 改为训练 / 评估分离后，召回率跌到 0，暴露了真实的类别不平衡问题（默认阈值 0.5 下模型全判合格）；

* 通过 "SMOTE 过采样 + 限制模型复杂度 + 自定义阈值 0.20" 三管齐下，得到召回率 0.75、准确率 0.60 的真实指标。

这个调试过程让我理解了评估方法比模型本身更关键 —— 指标必须是模型在没见过数据上的表现才有意义。

**4. 依赖版本兼容问题**

安装 imbalanced-learn 后出现 `numpy.dtype size changed` 报错，是 NumPy 与 Pandas 底层二进制接口不兼容。通过固定兼容版本组合（numpy 1.23.5 /pandas 1.5.3 /scikit-learn 1.2.2 /imbalanced-learn 0.10.1）解决。

### 6.3 功能测试



| 测试项                  | 结果                  |
| -------------------- | ------------------- |
| 首页 / 批次列表 / 批次详情页面访问 | 正常，无 404            |
| 批次列表加载               | 正确加载 1567 条数据       |
| 模型训练                 | 一键训练正常执行，输出三类模型指标   |
| 单批次智能分析              | 流程完整，三项结果正确展示并写入数据库 |



***

## 七、总结与展望

### 7.1 设计总结

本设计完成了半导体产线智能质量预警系统的全流程开发：从 SECOM 原始数据的清洗、特征筛选、标准化与入库，到三张数据库表的设计，再到 PCA、孤立森林、随机森林三类算法的训练与集成，最后以 Web 页面的形式把 "降维可视化 — 异常预警 — 质量判定" 完整呈现出来。系统不是单纯的算法演示，而是数据、算法、界面三层的工程化闭环，符合智能制造课程 "工业数据预处理、机器学习预测、工业数据可视化" 三个核心技术方向的要求。

### 7.2 不足与展望



1. **数据时效性**：SECOM 是较早的公开数据集，与当前产线工艺存在差异；后续可接入真实产线数据验证；

2. **特征工程**：目前特征主要来自原始传感器列，可进一步做特征选择（如按特征重要性筛选）或构造交互特征，提升模型上限；

3. **模型对比**：当前以随机森林为主，可补充 XGBoost、LSTM 等模型做对比实验，验证选型的合理性；

4. **在线能力**：目前是离线训练 + 单批次分析，后续可扩展实时数据接入与在线增量学习，实现真正的产线实时预警；

5. **工程完善**：可增加用户管理、权限控制、报表导出等功能，提升系统完整度。



***

*本说明书内容与项目代码、数据库实际数据一致，实验数据均可通过&#x20;*`src/algorithms/`*&#x20;下脚本复现。*